# Development Workflow

This repository follows a harness-oriented loop:

1. Initialize the session with `./harness/init.sh`.
2. Read the current repo state from `state/`.
3. Choose one unfinished feature.
4. Implement only that feature.
5. Run verification before claiming completion.
6. Update progress and handoff notes.

Keep requirements, implementation, and verification explicit in the repo.
